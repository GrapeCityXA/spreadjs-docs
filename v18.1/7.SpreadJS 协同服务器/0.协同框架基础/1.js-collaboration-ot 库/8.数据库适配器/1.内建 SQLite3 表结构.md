# sqlite3-schema

## SQL

```sql
CREATE TABLE IF NOT EXISTS documents (
  id TEXT PRIMARY KEY,
  type TEXT NOT NULL,
  version INTEGER NOT NULL,
  snapshot_version INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS operations (
  doc_id TEXT NOT NULL,
  version INTEGER NOT NULL,
  operation TEXT NOT NULL,
  PRIMARY KEY (doc_id, version),
  FOREIGN KEY (doc_id) REFERENCES documents (id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS snapshot_fragments (
  doc_id TEXT NOT NULL,
  fragment_id TEXT NOT NULL,
  data TEXT NOT NULL,
  PRIMARY KEY (doc_id, fragment_id),
  FOREIGN KEY (doc_id) REFERENCES documents (id) ON DELETE CASCADE
);
```

## 模式

| **表名（Table Name）** | **列名（Column Name）** | **数据类型（Data Type）** | **描述（Description）** |
| -------------- | --------------- | --------------- | --------------- |
| @cols=1:@rows=4:documents | id | 文本（TEXT） | 文档的唯一标识符 |
| version | 整数（INTEGER） | 文档的版本号 |
| snapshotVersion | 整数（INTEGER） | 快照版本号 |
| type | 文本（TEXT） | 文档的类型 |
| @cols=1:@rows=3:ops | roomId | 文本（TEXT） | 房间 ID |
| version | 整数（INTEGER） | 操作的版本号 |
| data | 文本（TEXT） | 与操作相关的数据 |
| @cols=1:@rows=3:fragments | roomId | 文本（TEXT） | 房间 ID |
| fragmentId | 文本（TEXT） | 片段的唯一标识符 |
| data | 文本（TEXT） | 与片段相关的数据 |
