# showRow

### 问题：如何将滚动条调整到初始位置

***

#### 背景：

用户在表格上填充数据，此时数据在可见屏幕中展示不完，这时候会有垂直滚动条，将滚动条拉到最后，然后重新填充数据，重新填充完成以后滚动条的位置任然是最下方，显示的数据也是最下方的数据，所以用户填充数据后想重置滚动条位置到开始位置。

```
//将滚动条调整到初始位置
sheet.showRow(0)
```