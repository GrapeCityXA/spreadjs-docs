# arrayform

公式可以包括对数组进行运算的函数。 SpreadJS支持公式中的数组常量。 使用大括号{}将数组元素括起来。 使用逗号分隔行中的元素。 使用分号分隔数组中的行。 各个元素可以是数字值，文本值，逻辑值或错误值。 数组的一些示例是：

CORREL({5,10,15,20,25},{4,8,16,32,64})

CORREL({73000,45000,40360},{42,70,40})

ROWS({1,2,3;4,5,6})

