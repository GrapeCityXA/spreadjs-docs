# T

此函数返回值引用的文字。



#### 语法

T( *value* )



#### 参数

要测试的值。



#### 注释

如果值是文字或引用文字，则 T 返回值。 如果值未引用文字，则 T 返回 ""（空文字）。



#### 数据类型

接受单元格引用。返回字符串数据。



#### 示例

T(B3) 如果B3包含“Test”，则该函数返回“Test”。

T(R3C2)

T(A1)

