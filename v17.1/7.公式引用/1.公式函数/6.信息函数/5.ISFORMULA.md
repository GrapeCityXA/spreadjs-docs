# ISFORMULA

此函数检验是否存在包含公式的单元格引用，然后返回 TRUE 或 FALSE。



#### 语法

ISFORMULA( *reference* )



#### 参数

对要测试单元格的引用。



#### 注释

如果引用一个空单元格或没有数据，则此函数返回FALSE。

此函数用于测试单元格的内容。



#### 数据类型

接受单元格引用作为参数。返回布尔（TRUE或FALSE）数据。



#### 示例

ISFORMULA(B4) 返回 false, 其中B4是具有数字值56的单元格引用。

