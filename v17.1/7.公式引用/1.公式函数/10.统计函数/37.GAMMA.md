# GAMMA

此函数返回 gamma 函数值。



#### 语法

GAMMA( *value* )



#### 参数

参数为>=1 的实数。



#### 注释

如果 value 为负整数或 0，则 GAMMA 返回 错误值 #NUM!。

如果 value 包含无效的字符，则 GAMMA 返回 错误值 #VALUE!。



#### 数据类型

接受数值数据。返回数值数据。



#### 示例

GAMMA(7.5) 得出结果 1871.254

GAMMA(1) 得出结果 1

GAMMA(-1.23) 得出结果 #NUM!

