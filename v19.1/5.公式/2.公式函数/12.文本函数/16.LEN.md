# LEN

此函数返回字符串中的字符数或存储变量所需的字节数。



#### 语法

LEN( *value* )



#### 参数

参数是您要查找其长度的文本。将空格视为字符。

参数必须是字符串或对字符串值的单元格引用。



#### 数据类型

接受字符串数据。返回数值数据。



#### 示例

LEFT(A2,LEN(A2)–1)

LEN(""FarPoint Technologies, NC"") 得出结果 25

