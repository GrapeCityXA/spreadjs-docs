# databasefunctions

数据库函数将数学或统计运算应用于被视为数据库的一系列单元格中值的子集。 可以将数据库表视为组织为行和列的二维数组。 或者可以将其视为记录的一维数组，其中每个记录都是具有一个或多个字段的结构。 在数据库表的上下文中，术语"row" 和 "record"表示相同的事物，术语 "column" 和 "field"表示相同的事物。 数据库是指一个单元格区域，其中范围的第一行代表字段标签。 范围中的其余行代表记录。 范围中的列代表字段。

