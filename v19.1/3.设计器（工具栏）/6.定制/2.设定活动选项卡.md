# set-active-ribbon

SpreadJS Designer 组件允许您在初始化设计器组件时设置或获取默认的活动功能区选项卡。
为此，将 `IDesignerConfig` 接口的 `ribbon[].active` 属性的值设置为true，或使用设计器组件的 `activeRibbonTab` 方法。
默认情况下，在SpreadJS Designer组件初始化时，HOME是活动选项卡。

> type=warning
> 注意：
> 如果设置为活动状态的功能区选项卡已被隐藏，则该选项卡将不可见。
> 如果多个功能区选项卡都被设置为活动状态，则SpreadJS仅将最后一个可见的选项卡视为活动状态。

以下代码片段展示了如何将 INSERT 选项卡设置为活动功能区选项卡

```javascript
// Use the active property in IRibbonPanel to set the active ribbon tab.
var config = GC.Spread.Sheets.Designer.DefaultConfig;
config.ribbon[1].active = true;
var designer = new GC.Spread.Sheets.Designer.Designer(document.getElementById("designerHost"), config);
```

或

```javascript
// Use the activeRibbonTab method of the designer component to set the active ribbon tab.
var config = GC.Spread.Sheets.Designer.DefaultConfig;
var designer = new GC.Spread.Sheets.Designer.Designer(document.getElementById('designerHost'), config);
let currentActiveRibbonTab = designer.activeRibbonTab();
if (currentActiveRibbonTab !== 'insert') {
    designer.activeRibbonTab('insert');
}
```

![ActiveRibbonTab.8c2209](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/5fefd5d8-238b-4c13-893b-6f22fcc5b9dd/ActiveRibbonTab.8c2209.997bc2.png)