# AddingtheConnectorShapes

连接线图形可借助线条、箭头以及预设连接点，将两个或多个图形连接起来。SpreadJS 原生支持在工作表中创建并自定义连接线图形。
你可以借助连接线，在各类图表与流式布局中直观展现图形之间的关联关系。

## 连接线类型

SpreadJS 支持以下几种连接线类型：

1. 直线连接线：通过直线完成图形之间的连接。
2. 折线连接线：利用折角线段连接图形。
3. 曲线连接线：使用曲线连接图形。

你还可以自定义连接线的箭头样式，具体包含：

* 普通样式（无箭头）
* 基础双向箭头
* 开口箭头
* 隐形箭头
* 椭圆形箭头
* 菱形箭头

下图为各类连接线图形示意图：
![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/0f73f140-2ccc-4566-a244-10944255eeb4/image-20260310.bfd676-20260525.16203e.png?width=600)

## 连接模式

连接线图形依靠预设的连接点附着在各类图形上。
每一条连接线都包含：

* 起始连接端
* 结束连接端

这些端点能够绑定至指定图形以及对应序号的连接点，相关代码示例如下：

```javascript
connectorShape.startConnector({ name: shape1.name(), index: 3 });
connectorShape.endConnector({ name: shape2.name(), index: 2 });
```

## 连接线交互与吸附特性

在工作表内使用连接线时，SpreadJS 内置了完善的交互逻辑。

### 端点显示效果

* 选中连接线时：
    * 已完成连接的端点会显示为实心绿色圆点
    * 未连接的端点显示为白色空心圆点
* 拖动连接线的过程中，端点圆点会暂时隐藏，减少视觉干扰
* 拖动操作结束后，端点标识会自动恢复显示

### 自动吸附功能

将连接线端点拖动至有效连接点附近时：

* 连接线会自动吸附到距离最近的连接点上
* 该功能可有效提升连接精准度与操作效率

吸附过程中具备以下效果：

* 目标连接点会出现绿色高亮光晕，作为操作视觉反馈
* 实时预览连接线走向，用户可在松开鼠标前确认最终排布路径
    ![connector.gif](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/0f73f140-2ccc-4566-a244-10944255eeb4/connector-20260309.42577d-20260525.748b07.gif?width=600)

>type=info
> 目前高亮颜色和光晕半径为固定设置，暂不支持自定义修改。

## 示例：添加连接线图形

以下示例演示如何在两个图形之间添加连接线：

```javascript
// 在工作表中添加连接线图形
window.onload = function ()
{   
var spread = new GC.Spread.Sheets.Workbook(document.getElementById("ss"));   
var sheet = spread.getActiveSheet();
   // 新建两个基础图形   
var shape1 = sheet.shapes.add("myShape1", GC.Spread.Sheets.Shapes.AutoShapeType.rectangle, 150, 100, 100, 100);   
var shape2 = sheet.shapes.add("myShape2", GC.Spread.Sheets.Shapes.AutoShapeType.oval, 700, 100, 100, 100);
   // 添加连接线   
var connectorShape = sheet.shapes.addConnector(       
"myConnectorShape",       
GC.Spread.Sheets.Shapes.ConnectorType.straight,       
250, 150, 700, 150   
);
   // 设置连接线样式   
var oldStyle = connectorShape.style();   
oldStyle.line.color = 'red';   
oldStyle.line.lineStyle = GC.Spread.Sheets.Shapes.PresetLineDashStyle.dot;   
oldStyle.line.width = 10;   
oldStyle.line.capType = GC.Spread.Sheets.Shapes.LineCapStyle.square;   
oldStyle.line.joinType = GC.Spread.Sheets.Shapes.LineJoinStyle.miter;   
oldStyle.line.beginArrowheadStyle = GC.Spread.Sheets.Shapes.ArrowheadStyle.triangle;   
oldStyle.line.beginArrowheadWidth = GC.Spread.Sheets.Shapes.ArrowheadWidth.narrow;   
oldStyle.line.beginArrowheadLength = GC.Spread.Sheets.Shapes.ArrowheadLength.short;   
oldStyle.line.endArrowheadStyle = GC.Spread.Sheets.Shapes.ArrowheadStyle.triangle;   
oldStyle.line.endArrowheadWidth = GC.Spread.Sheets.Shapes.ArrowheadWidth.medium;   
oldStyle.line.endArrowheadLength = GC.Spread.Sheets.Shapes.ArrowheadLength.medium;   
connectorShape.style(oldStyle);
   // 绑定对应连接点   
connectorShape.startConnector({ name: shape1.name(), index: 3 });   
connectorShape.endConnector({ name: shape2.name(), index: 2 });
};
```

## 使用限制

SpreadJS 暂不支持以下使用场景：

* 不支持自由绘制排布连接线路径
* 连接线的排版布局效果可能与 Excel 存在差异
* 使用弯折箭头类连接线时，Excel 会出现画面裁切现象，而 SpreadJS 可完整保留填充样式展示效果
* 调整标注框、气泡框、云朵标注等部分图形大小时，容易出现画面显示裁切问题
* 将立方体、斜条纹等图形的控制点调整至极限数值时，大概率会出现显示异常效果