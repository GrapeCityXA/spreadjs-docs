# TODAY

这个函数返回当前日期的日期和时间。



#### 语法

TODAY()



#### 参数

此函数没有参数。



#### 注释

如果在日期时间单元格(DateTimeCellType)中使用此函数，则单元格将使用日期格式设置对值进行格式化。

只有在重新计算包含该函数的表单或单元格时，该函数才会更新。这是一个易变函数。



#### 数据类型

不接受数据。返回DateTime对象。



#### 示例

如果今天是2003年11月14日，那么

TODAY() 得出结果 November 14, 2003 12:00:00AM

