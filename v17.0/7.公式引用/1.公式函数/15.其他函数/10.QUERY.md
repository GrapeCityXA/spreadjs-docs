# query-function

此函数从工作表中的数据管理器表返回数据。



#### 语法

QUERY( *tableAndRows* , *columns* )



#### 参数列表

QUERY函数有下列参数：

| 参数名 | 描述 |
|--- |--- |
| tableAndRows | 表格名称和行。 |
| columns | [可选]字段名称。 |



#### 数据类型

接受字符串数据类型。

返回以下类型的数据：

| 返回数据类型 | 示例 |
|--- |--- |
| 整个表格 | QUERY("table1") |
| 整个列 | QUERY("table1", "column1") |
| 数据 | QUERY("table1#1", "column1")<br />QUERY("customer/abc@gmail.com", "name) |
| 某行 | QUERY("table1#1")<br />QUERY("customer/abc@gmail.com") |



#### 说明

用户可以使用以下简单的行筛选：

| 类型 | 示例 |
|--- |--- |
| 按行索引 | QUERY("table1#1") |
| 按行主键 | QUERY("customer/abc@gmail.com") |
| 按键值筛选 | QUERY("order?status=success") |

用户可以通过使用数组来选择多列。

| 类型 | 示例 |
|--- |--- |
| 按列数组 | QUERY("order?status=shipping", {"order date", "address", "Cargo weight"}) |
| 按单向引用 | QUERY("order?status=shipping", A1:D1) |



#### 示例

下面的GIF说明了使用QUERY函数根据税率显示项信息。

![](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/041236cd-4159-4f89-9e42-7be2bb789d22/images/ts-queryfunction.gif)

以下代码示例演示如何在工作表中实现QUERY函数。


```JavaScript
// 创建一个新工作表
var sheet1 = new GC.Spread.Sheets.Worksheet();
sheet1.name("TS Reference in Formula");
spread.addSheet(0, sheet1);
spread.options.allowDynamicArray = true;
                
// 唯一税率
sheet1.setValue(3, 0, 'Different Tax Rates are:-')
sheet1.setFormula(3, 1, '=UNIQUE(TableSheet1[Tax Rate])'); // it returns a dynamic array and we have already set allowDynamicArray to true initially
// 设置唯一值的数据验证器
var dv = GC.Spread.Sheets.DataValidation.createFormulaListValidator('=$B$4#');
sheet1.setDataValidator(3, 3, dv);
//从数据验证器中设置单元格中的值
sheet1.setValue(3, 3, 20);
//应用 tableStyle
applyTableStyleForRange(sheet1, 5, 3, 400, 3);
sheet1.setValue(3, 2, 'Select a tax rate to Query the TableSheet')
sheet1.setArray(5, 3, [["stockItem", "unitPrice", "stockItemKey"]]);
// 使用QUERY函数
sheet1.setFormula(6, 3, '=QUERY("myTable?taxRate="&D4, D6:F6)');
```

