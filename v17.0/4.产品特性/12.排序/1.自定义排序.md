# custom-sorting

在几种情况下，仅以简单的升序或降序排序是不够的。 例如，为了根据XXS，XS，S，M，L，XL，XXL等不同尺寸对衣服列表进行排序，您可能需要先比较这些项目，然后再对其进行排序。 为了实现这种类型的排序（也称为自定义排序），SpreadJS在事件信息中提供了 **Events.RangeSorting** 和 **compareFunction** 。

以下gif图描述了对服装尺码进行的自定义排序。

![](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/041236cd-4159-4f89-9e42-7be2bb789d22/images/custom-sorting.gif)



#### 示例代码

本示例根据尺寸对男士服装列表进行排序。


```JavaScript
// 初始化 Spread
var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'), { sheetCount: 1 });
// 获取表单
var activeSheet = spread.getActiveSheet();
// 设置数据
activeSheet.setArray(1, 1, [
["Men's clothing sizes"], [],
["XXS", "166–170"],
["XS", "168–173"],
["S", "171–176"],
["M", "174–179"],
["L", "177–182"],
["XL", "180–184"],
["XXL", "182–186"],
["3XL", "184–188"],
["4XL", "187–190"]]);
// 在这里，我们将衣服的尺寸从XXS尺寸分类到7XL尺寸。
// 比较项目并对其进行排序的功能
function compareList(obj1, obj2) {
var list = ["XXS", "XS", "S", "M", "L", "XL", "XXL", "3XL", "4XL", "5XL", "6XL", "7XL"];
var index1 = list.indexOf(obj1), index2 = list.indexOf(obj2);
if (index1 > index2) {
    return 1;
} else if (index1 < index2) {
    return -1;
} else {
    return 0;
}
}
activeSheet.rowFilter(new GC.Spread.Sheets.Filter.HideRowFilter(new GC.Spread.Sheets.Range(3, 1, 9, 2)));
activeSheet.bind(GC.Spread.Sheets.Events.RangeSorting, function (e, info) {
info.compareFunction = compareList;
});
```

在自定义UI排序中，不支持颜色（样式）的排序。
> **注意:** 在自定义UI排序中，不支持颜色（样式）的排序。 
