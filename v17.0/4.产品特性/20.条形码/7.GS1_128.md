# SettingGS1_128

GS1-128是使用一系列应用程序标识符对数据进行编码的条形码。 该条形码使用完整的ASCII字符集，同时还使用FNC1字符作为第一个字符位置。

此条形码特别用于日期，批号，重量和HIBC应用等。

下面共享了描绘GS1-128条码迷你图的截图。

![](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/041236cd-4159-4f89-9e42-7be2bb789d22/images/gs1128.png)



#### 公式定义

您可以使用以下公式在表单中设置GS1-128迷你图：

=BC_GS1_128(value, color, backgroudColor, showLabel, labelPosition, fontFamily, fontStyle, fontWeight, fontTextDecoration, fontTextAlign, fontSize, quietZoneLeft, quietZoneRight, quietZoneTop, quietZoneBottom)



#### 参数

| 名称 | 描述 |
|--- |--- |
| value | 表示条形码编码的数据 |
| color | 条形码的字体颜色(默认值是 'rgb(0,0,0)') |
| backgroundColor | 条形码的背景色(默认值是'rgb(255, 255, 255)')。 |
| showLabel | 是否显示条形码标注 |
| labelPosition | ​条形码标注的位置 |
| fontFamily | 一个字符串，代表标签文本fontFamily。 默认值为“ sans-serif”。 |
| fontStyle | 条形码标记的字体样式(默认值是 'normal') |
| fontWeight | 条形码标记的字体粗细(默认值是 'normal') |
| fontTextDecoration | 条形码标记的装饰线(默认值是 'none') |
| fontTextAlign | 条形码标记的文字排列(默认值是'center') |
| fontSize | 一个字符串，代表标签文本fontSize。 默认值为“ 12px”。 |
| quietZoneLeft | 条形码左侧静默区的大小 |
| quietZoneRight | 条形码右侧静默区的大小 |
| quietZoneTop | 条形码上侧静默区的大小 |
| quietZoneBottom | 条形码下侧静默区的大小 |



#### 示例代码

此示例代码在表单中设置GS1_128迷你图。


```JavaScript
var sheet = spread.getActiveSheet();
// 设置GS1_128条码迷你图
sheet.setValue(3, 1, "gs1128", GC.Spread.Sheets.SheetArea.viewport);
sheet.setValue(3, 2, "235465143135", GC.Spread.Sheets.SheetArea.viewport);
// 设置样式
sheet.setRowHeight(3, 100);
for (var col = 1; col < 4; col++)
{
sheet.setColumnWidth(col, 200);
}
sheet.getRange(3, 0, 1, 4).hAlign(GC.Spread.Sheets.HorizontalAlign.center);
sheet.getRange(3, 0, 1, 4).vAlign(GC.Spread.Sheets.VerticalAlign.center);
// 设置公式
sheet.setFormula(3, 3, '=BC_GS1_128(C4)');
```

