# oadate-convert

### 问题：OADate在不同环境下的转换问题

***

#### 背景：

在SpreadJS中使用日期格式在导出JSON，数据绑定获取内容的时候会出现OADate的情况，当单元格的值是一个日期时，SpreadJS会将其存储为OADate。很多时候用户并不知道该如何将OADate转换成一般的日期格式，下面是不同的编程环境下的应对方案。

#### 解决方案：

JavaScript前端的解决方案：

```js
function fromOADate(date) {
  var oaDateReg = new RegExp(
    "^/OADate\\(([-+]?(\\d+(\\.\\d*)?|\\.\\d+)([eE][-+]?\\d+)?)\\)/\\s*$"
  );

  if (typeof date === "string" && oaDateReg.test(date)) {
    var oadate = parseFloat(date.match(oaDateReg)[1]);
    var ms =
      (oadate * 86400000 * 1440 -
        25569 * 86400000 * 1440 +
        new Date((oadate - 25569) * 86400000).getTimezoneOffset() * 86400000) /
      1440;
    return new Date(ms);
  } else {
    return date;
  }
}
```

![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.e1259e.png)

另外，还有一个取巧的办法就是通过tag进行一个转换。

```js
sheet.tag("/OADate(44542)/");
var date = sheet.tag();
```

![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.db067c.png)

Java后端环境下的解决方案：

```js
long d = 44542;
double mantissa = d - (long) d;
double hour = mantissa * 24;
double min = (hour - (long) hour) * 60;
double sec = (min - (long) min) * 60;


SimpleDateFormat myFormat = new SimpleDateFormat("dd MM yyyy");
Date baseDate = myFormat.parse("30 12 1899");
Calendar c = Calendar.getInstance();
c.setTime(baseDate);
c.add(Calendar.DATE, (int) d);
c.add(Calendar.HOUR, (int) hour);
c.add(Calendar.MINUTE, (int) min);
c.add(Calendar.SECOND, (int) sec);

System.out.println(c.getTime());
```

![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.4ec76e.png)

.net环境下的解决方案：
`System.DateTime.FromOADate(44542);`

![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.02fb5c.png)

> 作者: GrapeCity China \| 审核：Clark\.Pan \| 更新时间：2023\.12\.10