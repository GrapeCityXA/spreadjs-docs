# hierarchical-cell-index

报表使用上下文来分析公式单元格。如果单元格包含上下文，在预览模式下溢出单元格的数据模型将是一棵树。
您可以使用某些报表公式在树中获取单元格的值、索引或排名。这些公式称为层级单元格索引。
分层单元格索引有三个重要功能：

* 获取值
* 获取索引
* 获取排名

此外，报表提供以下快捷函数，使用户简单地获取单元格值。

* R.MOM
* R.YOY
* R.PROPORTION
* R.CUMULATIVE