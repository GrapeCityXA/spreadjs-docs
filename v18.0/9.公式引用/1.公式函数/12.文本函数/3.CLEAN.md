# CLEAN

此函数删除文本中所有不能打印的字符。



#### 语法

CLEAN( *text* )



#### 参数

要从中删除非打印字符的任何表单信息。



#### 注释

对从其他应用程序导入的文本使用 CLEAN，将删除其中含有的当前操作系统无法打印的字符。 例如，可以使用 CLEAN 删除某些通常出现在数据文件开头和结尾处且无法打印的低级计算机代码。



#### 数据类型

接受字符串数据。返回字符串数据。



#### 示例

在此示例中，CHR（7）返回不可打印的字符

CLEAN(CHAR(7)&"text"&CHAR(7)) 得出结果 text

