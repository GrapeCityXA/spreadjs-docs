# LENB

此函数返回用于表示文本字符串中字符的字节数。



#### 语法

LENB(text_ *value* )



#### 参数

参数是您要查找其长度的文本。将空格视为字符。

参数必须是字符串或对字符串值的单元格引用。



#### 注释

只有在将 DBCS 语言设置为默认语言时，函数 LENB 才会将每个字符按 2 个字节计数。



#### 数据类型

接受字符串数据。返回数值数据。



#### 示例

LENB("rheabuto") 得出结果 8.

LENB("rosy") 得出结果 4.

