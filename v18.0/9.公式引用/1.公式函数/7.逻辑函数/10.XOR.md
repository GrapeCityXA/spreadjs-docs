# XOR

XOR函数返回所有参数的逻辑异或。



#### 语法

XOR( *value1,value2,..* )



#### 参数

最多提供255个参数，参数为数值(0或1)或逻辑值(true或false)。



#### 注释

当 TRUE 输入的数字为奇数时，XOR 的结果为 TRUE；当 TRUE 输入的数字为偶数时，XOR 的结果为 FALSE。



#### 数据类型

接受逻辑数据或数值(0或1)。返回逻辑数据。



#### 示例

XOR(3>0,2<9) 得出结果 TRUE

