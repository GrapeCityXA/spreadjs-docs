# prevent-drag

### 问题：如何禁止指定单元格无法被拖拽？

***

### 实现思路

#### 1、表单保护

用表单保护来实现的话需要设置单元格的 `locked` 属性，如果要禁止的区域比较复杂的话，就会很麻烦。

#### 2、监听 DragDropBlock 事件

在此事件中判断 fromRow、fromCol、rowCount、colCount 是否与您的指定区域相符，相符则调用 cancel 取消拖拽操作。

```
var activeSheet = spread.getActiveSheet();
activeSheet.bind(GC.Spread.Sheets.Events.DragDropBlock, function (e, args) {
        console.log("From Column:" + args.fromCol);
        console.log("From Row:" + args.fromRow);
        console.log("To Column:" + args.toCol);
        console.log("To Row:" + args.toRow);
    });
```

> 作者: GrapeCity China \| 审核：AlexZ \| 更新时间：2023\.09\.27