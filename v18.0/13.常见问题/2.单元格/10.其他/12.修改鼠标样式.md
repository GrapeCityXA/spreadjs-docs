# custom-celltype-hover-icon

### 背景

***

想要在单元格数据悬浮时，如何显示自定义的样式，那么本次我们来实现鼠标悬浮出现图标的自定义单元格，我们先来看下具体效果。

![091633wab1j1vnl2bv0a91](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/091633wab1j1vnl2bv0a91.746ff9.gif)

### 实现思路

要实现鼠标悬浮和离开的效果，就需要用到 `processMouseEnter` 和 `processMouseLeave` 两个方法，前者是处理鼠标移入的方法，后者是处理鼠标移出的方法。我们来看下具体如何实现。
为了让创建的自定义单元格可以编辑，将继承 Text 这个cellType。

```javascript
function SortHearderCellType() {}
SortHearderCellType.prototype = new spreadNS.CellTypes.Text();
```

接着在 paint 方法里进行图标的绘制，并且创建一个flag，需要绘制时，将这个 flag 置为true；否则置为false。

```javascript
SortHearderCellType.prototype.paint = function (
  ctx,
  value,
  x,
  y,
  width,
  height,
  style,
  context
) {
  spreadNS.CellTypes.Text.prototype.paint.apply(this, arguments);
  if (this._mouseEnter) {
    style.backgroundImage = src;
    x = x + width / 2 + 70 / 2;
    y = y + (height - 16) / 2;
    width = 16;
    height = 16;
    value = "";
    spreadNS.CellTypes.Text.prototype.paint.apply(this, arguments);
  }
};
```

最后，到鼠标移入移出的逻辑了，其实两者都比较简单而且逻辑基本一致，只需将上面说的flag进行修改即可。
修改完成后，要调用 repaint 将表单重绘刷新一下。

```javascript
//鼠标移动
SortHearderCellType.prototype.processMouseEnter = function (hitInfo) {
  this._mouseEnter = true;
  var sheet = hitInfo.sheet;
  sheet.repaint(
    new GC.Spread.Sheets.Rect(
      hitInfo.cellRect.x,
      hitInfo.cellRect.y,
      hitInfo.cellRect.width,
      hitInfo.cellRect.height
    )
  );
}; //鼠标离开
SortHearderCellType.prototype.processMouseLeave = function (hitInfo) {
  this._mouseEnter = false;
  var sheet = hitInfo.sheet;
  sheet.repaint(
    new GC.Spread.Sheets.Rect(
      hitInfo.cellRect.x,
      hitInfo.cellRect.y,
      hitInfo.cellRect.width,
      hitInfo.cellRect.height
    )
  );
};
```

具体的Demo可以参考该[链接](https://gcdn.grapecity.com.cn/showtopic-99695-1-1.html)。

> 作者: GrapeCity China \| 审核：AlexZ \| 更新时间：2023\.09\.27