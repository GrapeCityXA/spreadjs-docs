# CustomerName

### 问题：如何获取命名区域数据？

***

#### 背景：

我们在实际项目中经常会用到SpreadJS的命名信息，在给一个区域设置命名名称后，如何获取一个这个区域的数据呢？

#### 解决方案：

1、首先给一个区域添加命名，来复现场景。

```
var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'));
   var sheet = spread.getSheet(0);
sheet.setArray(5, 2, [
            [ 'ID', 'Phone Number', 'Address' ],
            [ 1, '021-432378', 'Marbury Road' ],
            [ 2, '021-432668', 'Chester Road' ],
            [ 3, '021-432238', 'Gertt Road' ],
            [ 4, '021-432533', 'Jnyliner Road' ],
            [ 5, '021-432125', 'Approach Road' ],
            [ 6, '021-432789', 'Jones Road' ]
        ]);
        sheet.addCustomName("name1", "$C$6:$E$12", sheet.getActiveRowIndex(), sheet.getActiveColumnIndex(), "test1");
```

2、通过`getCustomNames`可以获取所有自定义名称信息，返回的是所有命名信息的数组`NameInfo`，再通过`NameInfo`的`getName`方法获取命名。

```
var name = sheet.getCustomNames()[0].getName();
```

3、通过`getCustomName`可以获取指定的自定义名称的命名信息，包含了名称、描述、行、列和备注信息。

```
var nameInfo = sheet.getCustomName(name);
```


4、最后通过`getArray`获取区域数据。

```
var row = nameInfo.getExpression().row;
        var col = nameInfo.getExpression().column;
        var rowCount = nameInfo.getExpression().endRow -nameInfo.getExpression().row+1;
        var colCount = nameInfo.getExpression().endColumn -nameInfo.getExpression().column+1;
        var nameData = sheet.getArray(row,col,rowCount,colCount); 
```

最后查看控制台，下图展示的是获取到的命名区域数据。
![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.cc8b68.png)

获取指定的自定义名称信息

> 作者: GrapeCity China \| 审核：RichardMa \| 更新时间：2023\.11\.15