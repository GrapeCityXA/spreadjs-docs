# jump

### 问题：当设置了行列冻结后，滚动条滚动到隐藏区域，这时候如何跳转到之前选中的单元格？

***

背景：用户在有行列冻结状态的表单中选择一个单元格后，随之滚动条滚动，之前选中的单元格被滑到隐藏区域，此时用户想要快速定位到之前的单元格，而不是想用滚动条向上翻查找。
可以使用以下两行代码

```
sheet.showCell(row,col)          
sheet.setActiveCell(row,col)
```