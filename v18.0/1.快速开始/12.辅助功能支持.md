# accessibility-support

SpreadJS为使用屏幕阅读器等辅助技术的残障用户提供了足够的无障碍支持。因此，使用该应用程序的开发人员将能够利用无障碍支持，为所有客户提供轻松无忧的终端用户体验。
网页上的文本内容可以通过以下方式访问：

* 按下Tab键可更改活动单元格。
* 移动鼠标可悬停在单元格上。
* 当示例页面处于活动选项卡时，会提供适当的文本。

为了使屏幕阅读器读出的文本更具意义，针对不同情况使用了不同文本，如下所示：

| **情况** | **屏幕阅读器读出的文本** |
| --- | ---------- |
| 按下Tab键，单元格(0, 1)成为活动单元格，其值为“name”。 | 单元格(0, 1)的值为“name” |
| 鼠标进入视口区域的单元格(0, 1)，其值为“name”。 | 单元格(0, 1)的值为“name” |
| 鼠标进入列标题区域的单元格(0, 1)，其值为“name”。 | 列标题单元格(0, 1)的值为“name” |
| 鼠标进入行标题区域的单元格(1, 0)，其值为“name”。 | 行标题单元格(1, 0)的值为“name” |
| 鼠标进入选项卡栏的调整大小栏。 | 调整大小 |
| 鼠标进入选项卡栏的第一个区域。 | 第一个 |
| 鼠标进入选项卡栏的上一个箭头区域。 | 上一个箭头 |
| 鼠标进入选项卡栏的下一个箭头区域。 | 下一个箭头 |
| 鼠标进入选项卡栏的最后一个区域。 | 最后一个 |
| 鼠标进入选项卡栏的上一个按钮区域。 | 上一个按钮 |
| 鼠标进入选项卡栏的下一个按钮区域。 | 下一个按钮 |
| 鼠标进入选项卡栏的工作表选项卡区域，其名称为“Sheet1”。 | 工作表选项卡“Sheet1” |
| 鼠标进入选项卡栏的新建工作表区域。 | 新建工作表 |
| 鼠标进入选项卡栏的空白区域。 | 空白 |
| 鼠标进入水平滚动条的左侧按钮区域。 | 滚动条左侧按钮 |
| 鼠标进入垂直滚动条的顶部按钮区域。 | 滚动条顶部按钮 |
| 鼠标进入滚动条的滑块按钮区域。 | 滚动条滑块按钮 |
| 鼠标进入水平滚动条的右侧按钮区域。 | 滚动条右侧按钮 |
| 鼠标进入垂直滚动条的底部按钮区域。 | 滚动条底部按钮 |
| 示例页面处于活动选项卡，且活动单元格为单元格(0, 1)，其值为“name”。 | 单元格(0, 1)的值为“name” |

客户可以将无障碍支持与Windows Narrator、适用于Windows的NVDA（非可视化桌面访问）以及适用于Mac OS X的VoiceOver等屏幕阅读应用程序配合使用。诸如“按下Tab键”、“鼠标悬停”和“活动选项卡中的页面”等一些功能，根据所使用的屏幕阅读器和浏览器的类型，其功能表现会有所不同。
以下表格展示了屏幕阅读器在各种互联网浏览器中的实时测试结果：

| **屏幕阅读器** | **浏览器** | **按Tab键** | **鼠标悬停** | **活动选项卡中的页面** |
| ----- | --- | ----- | ---- | --------- |
| 适用于Windows的NVDA | Chrome | 支持 | 支持 | 支持 |
| 适用于Windows的NVDA | Firefox | 支持 | 支持 | 不支持 |
| 适用于Windows的NVDA | Chromium Edge | 支持 | 支持 | 不支持 |
| 适用于Windows的NVDA | Edge | 不支持 | 不支持 | 不支持 |
| 适用于Windows的NVDA | IE | 不支持 | 不支持 | 不支持 |
| Windows Narrator | Chrome | 支持 | 支持 | 不支持 |
| Windows Narrator | Firefox | 支持 | 支持 | 不支持 |
| Windows Narrator | Chromium Edge | 支持 | 支持 | 不支持 |
| Windows Narrator | Edge | 支持 | 不支持 | 不支持 |
| Windows Narrator | IE | 支持 | 不支持 | 不支持 |
| 适用于OS X的VoiceOver | Chrome | 带有局限支持性 | 带有局限支持性 | 不支持 |
| 适用于OS X的VoiceOver | Safari | 带有局限支持性 | 带有局限支持性 | 不支持 |

> **注意**：适用于Mac OSX的VoiceOver阅读器在Chrome和Safari浏览器中受支持，但存在一些限制。当div元素有内容时，该阅读器会忽略aria - label属性。因此，对于“按下Tab键，单元格(0, 1)成为活动单元格，其值为“name”这种情况，VoiceOver阅读器只能识别单元格文本“name”。同样，适用于OSX的VoiceOver阅读器无法识别动态的aria - label属性，所以当鼠标悬停在canvas元素上或示例页面处于活动选项卡时，工作表会提供整个视口内容。此外，适用于OSX的VoiceOver阅读器无法识别选项卡栏、滚动条以及图片、图表和形状等浮动对象。

### 启用无障碍支持

尽管SpreadJS默认启用了无障碍功能，但不同的屏幕阅读器可能会因其功能不同而给出不同的阅读结果。若要禁用无障碍功能，用户需要将`enableAccessibility`选项设置为False。
以下代码示例禁用了无障碍支持：

```JavaScript
spread.options.enableAccessibility = false;
```

### 使用设计器

除了SpreadJS运行时，SpreadJS设计器也为用户提供了足够的无障碍支持。设计器上的用户界面元素将由屏幕阅读器以有意义的内容读出，以在SpreadJS设计器中启用无障碍支持。
要在SpreadJS设计器中启用无障碍支持，请按照以下步骤操作：

1. 在设计器功能区中，单击“设置”选项卡，然后从“Spread设置”中选择“常规”。
    ![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.14b541.png)
2. 这将打开“跨页设置”对话框。 单击“启用辅助功能”复选框。
    ![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.e48f70.png)

> **注意**：所有功能仅具备基本的无障碍支持。

### 为单元格设置替代文本

替代文本（Alt文本）是一种描述性文本，可帮助残障用户以及使用屏幕阅读器的用户与单元格内容进行交互。当屏幕阅读器读取带有替代文本的单元格时，它可以向用户大声读出替代文本，这有助于他们在不依赖视觉呈现的情况下理解单元格的内容。
当`enableAccessibility`选项设置为true时，SpreadJS支持为单元格设置替代文本，以便更好地实现内容的无障碍访问。除了纯文本，替代文本还包含占位符，例如{value}表示单元格值，{formatted}表示单元格格式化后的值。例如，如果单元格值为1000，替代文本为“Sales amount is {value}”，最终的无障碍内容将是“Sales amount is 1000”。
你可以根据需求设置、查看、复制、移动和修改单元格的替代文本。SpreadJS还支持以.ssjson或.sjs格式对单元格的替代文本进行[序列化](gcdocsite__documentlink?toc-item-id=adbb66e6-dd64-4a18-ad11-2b4a8bb62c76)和反序列化。
要设置或获取单元格的替代文本值，可以使用以下方法：

* [GC.Spread.Sheets.CellRange](gcdocsite__documentlink?toc-item-id=64f05fd4-fafa-47dc-9856-b74263f357ec) 类的`altText`方法。

```JavaScript
// 使用altText方法设置和获取单元格的替代文本值。
activeSheet.getCell(0, 0).altText("Set alternative text for the cell");
var B1 = activeSheet.getCell(1, 1).value(1000);
B1.altText("Alt Text is the cell value: " + activeSheet.getCell(1, 1).value());
console.log(activeSheet.getCell(0, 0).altText());
console.log(activeSheet.getCell(1, 1).altText());

activeSheet.setValue(0, 3, new Date(2013, 3, 1));
activeSheet.getCell(0, 3).formatter('d - mmm;@');
console.log("Cell formatter of cell[0,3] is :" + activeSheet.getCell(0, 3).formatter());
console.log("Formatted value of cell[0,3] is :" + activeSheet.getCell(0, 3).value());
```

* [GC.Spread.Sheets.Worksheet](gcdocsite__documentlink?toc-item-id=995f2b70-17d8-47a4-8e09-962f84b4dfc1) 类的`setAltText`和`getAltText`方法。

```auto
// 使用setAltText和getAltText方法，结合单元格值设置和获取替代文本值。
activeSheet.setAltText(0, 0, "Set alternative text for the cell ");
console.log(activeSheet.getAltText(0, 0));
```

使用 [StorageType ](gcdocsite__documentlink?toc-item-id=db7d19ea-a2d9-41cf-88d1-703e3f55b29a)枚举的altText选项，可以获取替代文本的存储数据类型。

```auto
// 使用StorageType枚举获取单元格的替代文本值。
activeSheet.getCell(0, 0).altText("Alternative text for the cell ");
activeSheet.clear(0, 0, 3, 3, GC.Spread.Sheets.SheetArea.viewport, GC.Spread.Sheets.StorageType.altText);
```

此外，你可以使用 [CopyToOptions ](gcdocsite__documentlink?toc-item-id=062648b1-8dc0-463a-bb14-ab7b5da3f9ff)枚举的altText选项来复制单元格的替代文本。

```auto
// 使用CopyToOption枚举复制单元格的替代文本值。
activeSheet.getCell(0, 0).altText("Alternative text for the cell ");
activeSheet.copyTo(0, 0, 1, 1, 2, 2, GC.Spread.Sheets.CopyToOptions.altText);
```

### 限制

行/列/工作表不支持替代文本。

### 使用设计器

你可以使用SpreadJS设计器查看或修改单元格的替代文本值。为此，在单元格的上下文菜单中选择“**替代文本…**”选项。这将打开“**单元格替代文本对话框**”，显示所选单元格的替代文本值。如有需要，你也可以更改此值。

> **注意**：仅当在“设置”>“常规”>“Spread设置”对话框中启用了 **启用辅助功能** 复选框时，“**替代文本…**”选项才会显示在上下文菜单中。

右键单击单元格时，上下文菜单中出现的“**替代文本...**”选项如下所示：
![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/image.a74ba6.png?width=470)

## 为图片、形状和图表设置自定义替代文本

对于图片、图表和形状等浮动对象这类图形内容，SpreadJS并未提供合适的替代文本。但用户可以提供替代文本，以便在无障碍支持中使用这些文本。为此，Picture、FloatingObject、ConnectorShape、GroupShape、Shape和Chart类都有**alt**方法。
以下代码示例为图片、形状和图表设置自定义替代文本。

```javascript
$(document).ready(function () {
    // 初始化Spread
    var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'), { sheetCount: 2 });
    // 获取活动工作表
    var sheet = spread.getSheet(0);
    // 启用无障碍功能
    spread.options.enableAccessibility = true;
    spread.suspendPaint();
    // 设置焦点
    spread.focus();
    // 更改与Tab键、Shift + Tab键相关的命令
    var commands = spread.commandManager();
    // TAB
    commands.register("moveToNextCellThenControl", GC.Spread.Sheets.Commands.moveToNextCellThenControl, GC.Spread.Commands.Key.tab, false, false, false, false);
    // SHIFT + TAB
    commands.register("moveToPreviousCellThenControl", GC.Spread.Sheets.Commands.moveToPreviousCellThenControl, GC.Spread.Commands.Key.tab, false, true, false, false);
    // 设置默认行高和列宽
    sheet.defaults.rowHeight = 50;
    sheet.defaults.colWidth = 150;
    // 设置默认水平对齐和垂直对齐
    var defaultStyle = sheet.getDefaultStyle();
    defaultStyle.hAlign = GC.Spread.Sheets.HorizontalAlign.center;
    defaultStyle.vAlign = GC.Spread.Sheets.VerticalAlign.center;
    defaultStyle.rowHeight = 50;
    defaultStyle.colWidth = 150;
    sheet.setDefaultStyle(defaultStyle);
    // 绑定数据源
    sheet.setDataSource(dataSource);
    // 获取另一个工作表1
    var sheet = spread.getSheet(1);
    // 为图表准备数据
    sheet.setValue(0, 1, "Q1");
    sheet.setValue(0, 2, "Q2");
    sheet.setValue(0, 3, "Q3");
    sheet.setValue(1, 0, "移动电话");
    sheet.setValue(2, 0, "笔记本电脑");
    sheet.setValue(3, 0, "平板电脑");
    for (var r = 1; r <= 3; r++) {
        for (var c = 1; c <= 3; c++) {
            sheet.setValue(r, c, parseInt(Math.random() * 100));
        }
    }
    // 添加柱状图
    var c1 = chart_columnClustered = sheet.charts.add('chart_columnClustered', GC.Spread.Sheets.Charts.ChartType.columnClustered, 50, 300, 300, 300, "A1:D4");
    c1.alt("这是一个柱状图");
    var chartArea = c1.chartArea();
    chartArea.border.color = "rgba(20, 119, 167, 1)";
    chartArea.border.width = 2;
    c1.chartArea(chartArea);
    // 添加芒果图片
    var p1 = sheet.pictures.add("p1", "mango.jpg", 500, 50, 200, 200);
    p1.alt("这是一张芒果图片");
    p1.borderColor("rgba(20, 119, 167, 1)");
    p1.borderWidth(2);
    p1.borderStyle("solid");
    // 添加云朵形状
    var sp1 = sheet.shapes.add("sp1", GC.Spread.Sheets.Shapes.AutoShapeType.cloud, 250, 50, 200, 200);
    sp1.alt("这是一个云朵形状");
    spread.resumePaint();
    // 绑定事件以设置替代文本
    function setAltText(collection, altText) {
        var success = false;
        collection.forEach(function (obj) {
            if (obj.isSelected()) {
                obj.alt(altText);
                success = true;
            }
        });
        return success;
    }
    var alternativeText = document.getElementById("alternativeText");
    document.getElementById("setAlternativeText").addEventListener("click", function () {
        var altText = alternativeText.value;
        var success = setAltText(sheet.pictures.all(), altText);
        if (success) {
            return;
        }
        success = setAltText(sheet.charts.all(), altText);
        if (success) {
            return;
        }
        setAltText(sheet.shapes.all(), altText);
    });
    spread.bind(GC.Spread.Sheets.Events.PictureChanged, function (event, args) {
        if (args.propertyName === "isSelected" && args.picture.isSelected()) {
            alternativeText.value = args.picture.alt();
        }
    });
    spread.bind(GC.Spread.Sheets.Events.FloatingObjectChanged, function (event, args) {
        var floatingObject = args.floatingObject;
        if (floatingObject && floatingObject instanceof GC.Spread.Sheets.Charts.Chart) {
            if (args.propertyName === "isSelected" && floatingObject.isSelected()) {
                alternativeText.value = floatingObject.alt();
            }
        }
    });
    spread.bind(GC.Spread.Sheets.Events.ShapeChanged, function (event, args) {
        if (args.propertyName === "isSelected" && args.shape.isSelected()) {
            alternativeText.value = args.shape.alt();
        }
    });
});
```

> **注意**：启用无障碍支持后，视障人士可以方便地访问数据。但是，诸如通过鼠标或触摸触发的拖动填充、筛选、触摸等用户界面操作仍然难以使用。