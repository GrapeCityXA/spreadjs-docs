# sorting-by-chinese-pinyin

### 问题：如何实现按汉字首个拼音字母升序排序？

***
#### 背景：
SpreadJS的中文排序是根据中文的ASCII码值。在某些情况下，想要根据汉字的拼音字母来进行排序。

#### 解决方案：
如果想要按照自定义的方式，比如拼音来排序，可以使用自定义排序来实现。

实现自定义排序有两种方法
1\. 调用sortRange时，sortInfo中添加compareFunction回调
如：

```js
function pinyinCompare(obj1, obj2) {
  return obj1.toString().localeCompare(obj2.toString(), "zh");
}
sheet.sortRange(1, 0, 8, 2, true, [
  { index: 0, ascending: true, compareFunction: pinyinCompare }, //中文排序
]);
```
上面的pinyinCompare即是我们自定义的比较方法。其中通过JavaScript提供的localeCompare方法，同时设置了"zh"的区域代码，这意味着我们使用中文的本地排序规则进行比较


2\. 在RangeSorting事件中注入
如：

```js
function compareSize(value1, value2) {
  //...
  //return -1;
  //return 1;
  return 0;
}
sheet.bind(GC.Spread.Sheets.Events.RangeSorting, function (e, info) {
  //if(info.range is suited) {
  info.compareFunction = compareSize; // set the customize function. //}
});
```

其中的compareSize你可以自定定义，也可以用上面的localeCompare

两种方式只要根据使用场景选择即可。


> 作者: GrapeCity China \| 审核：Richard.Ma \| 更新时间：2023\.09\.26