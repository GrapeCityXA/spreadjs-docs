# property-function

PROPERTY函数按属性解析对象。用户可以指定返回对象值的属性路径。

PROPERTY函数支持将返回对象解析为有效的属性值。例如，这个函数在[MultiColumn Picker](gcdocsite__documentlink?toc-item-id=2a4ec0ae-7edf-470e-bf25-ea15f2fc6ad9)中很有用.



#### 语法

PROPERTY( *data_expression, property_path* )



#### 参数

此函数有以下参数:

| 参数 | 描述 |
|--- |--- |
|  *data_expression*  | 引用对象数据，并接受单元格引用，其值为对象或对象函数的结果。 |
|  *property_path*  | 引用对象中的属性路径。 |



#### 备注

如果data_expression不是一个对象，PROPERTY函数将抛出#N/一个错误。如果在对象中找不到property_path，函数还会抛出#N/一个错误。



#### 数据类型

接受字符串数据。



#### 示例


```JavaScript
<script>
        $(document).ready(function () {
            //初始化spread
            var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'), { sheetCount: 1 });
            //获取活动表
            var sheet = spread.getActiveSheet();
            // 数据源是数组
            let dataSource = [
                {
                    "NAME": "James Smith",
                    "POSITION": "AVP",
                    "OFFICE": "Fox Studios",
                    "EXTN": 71,
                    "START_DATE": '3/08/2008',
                    "JOINING_YEAR": 2008
                }
            ];
            let colInfos = [
                { name: "NAME", displayName: "NAME", size: "2*" },
                { name: "POSITION", displayName: "POSITION", size: "*" },
                { name: "OFFICE", size: "*" },
                { name: "EXTN", size: "*" },
                { name: "START_DATE", size: 100 },
                { name: "JOINING_YEAR", size: 100 }
            ];
            // 创建样式
            var style = new GC.Spread.Sheets.Style();
            style.cellButtons = [
                {
                    imageType: GC.Spread.Sheets.ButtonImageType.dropdown,
                    command: "openMultiColumn",
                    useButtonStyle: true,
                }
            ];
            style.dropDowns = [
                {
                    type: GC.Spread.Sheets.DropDownType.multiColumn,
                    option: {
                        width: 600,
                        height: 150,
                        dataSource: dataSource,
                        bindingInfos: colInfos
                    }
                }
            ];
            // 设置样式的格式
            style.formatter = '=PROPERTY(@, "OFFICE")';
            // 设置样式
            sheet.setStyle(0, 1, style);
            // 设置文本
            sheet.setText(0, 0, "数据源是数组, return data parsed to the value of property 'OFFICE'.");
            // 设置自动换行
            sheet.getCell(0, 0).wordWrap(true);
            sheet.getCell(0, 1).wordWrap(true);
            //设置列宽
            sheet.setColumnWidth(0, 200);
            sheet.setColumnWidth(1, 400);
            // 设置行高
            sheet.setRowHeight(0, 70);
        });
    </script>
```



#### See Also

[HLOOKUP](gcdocsite__documentlink?toc-item-id=643bf7c3-c84a-4938-9c6e-11944bf2265b)|[LOOKUP](gcdocsite__documentlink?toc-item-id=2158763e-40ad-4a9e-b063-bd95ea3d2c66)

