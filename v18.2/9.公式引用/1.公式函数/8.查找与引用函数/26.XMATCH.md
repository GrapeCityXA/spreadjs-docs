# XMATCH

函数在数组或单元格范围中搜索特定项，然后返回项的相对位置。换句话说，这个函数可以用来查找列表中某个项的位置。

XMATCH函数支持近似匹配、部分匹配(使用widcard字符，如*、?等)和精确匹配。



#### 语法

XMATCH *(lookup_value, lookup_array, [match_mode], [search_mode])* 



#### 参数

此函数有以下参数:

| 参数 | 描述 |
|--- |--- |
|  *lookup_value*  | 引用查找值。 |
|  *lookup_array*  | 引用要搜索的查找数组或单元格范围。 |
|  *[match_mode]*  | (可选)根据以下值指定匹配的类型:<br /><br />0 -  指精确匹配。如果没有找到匹配项，则返回#N/A 错误。这是默认的匹配模式。<br /><br />-1 - 指精确匹配。如果没有找到匹配项，则返回下一个较小的项。<br /><br />1 -  指精确匹配。如果没有找到匹配项，则返回下一个较大的项。<br /><br />2 -  引用一个通配符匹配，其中*，?字符具有表示部分匹配的特殊含义。<br /><br /> |
|  *[search_mode]*  | (可选) 按以下值指定搜索模式:<br /><br />0 -  指的是将返回所有匹配值的“搜索全部”模式。[此模式在Excel中不可用。]<br /><br />1 -  引用从第一项开始的搜索。这是默认的搜索模式。<br /><br />-1 - 指从最后一项开始的反向搜索。<br /><br />2  -  引用依赖于lookup_array参数按升序排序的二分查找。<br /><br />-2 -  引用依赖于lookup_array参数按降序排序的二分查找。<br /><br /> |



#### 备注

XMATCH函数可以处理垂直数组和水平数组。如果没有找到查找值，该函数将返回#N/A错误。如果同时使用通配符匹配模式和二分查找模式，XMATCH将返回#VALUE! 。



#### 数据类型

接收数字数据。查找范围或表中的值。返回具有多个项的数组。



#### 示例

下面的示例代码展示了XMATCH函数的用法。


```JavaScript
$(document).ready(function () {
    //初始化spread
    var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'), { sheetCount: 1 });
    // 启动动态数组支持
    spread.options.allowDynamicArray = true;
    //获取活动表
    var sheet = spread.getSheet(0);
    //设置列宽
    sheet.setColumnWidth(6, 160);

    // 创建公式
    var formula_Exact = '=XMATCH(G4,A5:A9)';
    var formula_Approx = '=XMATCH(G10,D5:D9,1)';
    var formula_MultipleValues = '=XMATCH({5,4,1},C5:C9)';

    // 设置值
    sheet.setValue(0, 6, 'Basic exact match');
    sheet.setValue(1, 6, formula_Exact);
    sheet.setValue(6, 6, 'Basic approximate match');
    sheet.setValue(7, 6, formula_Approx);
    sheet.setValue(12, 6, 'MultipleValues');
    sheet.setValue(13, 6, formula_MultipleValues);
    // 创建数据
    var data = [
        ["Movie", "Year", "Rank", "Sales"],
        ["Fargo", 1996, 5, 61],
        ["L.A. Confidential", 1997, 4, 126],
        ["The Sixth Sense", 1999, 1, 673],
        ["Toy Story", 1995, 2, 362],
        ["Unforgiven", 1992, 3, 159]
    ];
    // 设置数据
    sheet.setArray(3, 0, data);
    // Set value
    sheet.setValue(3, 5, 'Movie');
    sheet.setValue(4, 5, 'Position');
    sheet.setValue(3, 6, 'Toy Story');
    // 设置公式
    sheet.setFormula(4, 6, formula_Exact);

    // 设置值
    sheet.setValue(9, 5, 'Sales');
    sheet.setValue(10, 5, 'Position');
    sheet.setValue(9, 6, 400);
    // 设置公式
    sheet.setFormula(10, 6, formula_Approx);

    // 设置值
    sheet.setValue(15, 5, 'Rank');
    sheet.setValue(16, 5, 'Position');
    sheet.setValue(15, 6, '{5,4,1}');
    // 设置公式
    sheet.setFormula(16, 6, formula_MultipleValues);

});
```



#### See Also

[HLOOKUP](gcdocsite__documentlink?toc-item-id=643bf7c3-c84a-4938-9c6e-11944bf2265b)|[LOOKUP](gcdocsite__documentlink?toc-item-id=2158763e-40ad-4a9e-b063-bd95ea3d2c66)

