# CreatingRadarChart

雷达图（也称为蜘蛛图）是一种二维图表，可用于以图形格式表示按行和列绘制的多变量数据。
SpreadJS 在电子表格中支持雷达图，使用户能够进行动态数据比较并开展全面的信息分析。此外，通过对两个或多个数据系列的合计值进行深入评估，雷达图使信息的可视化和处理变得更加轻松快捷。

## 使用雷达图

雷达图通常用于在一系列特征上比较两个或多个实体。在雷达图中，三个或更多的定量变量在轴上表示，每个类别都有其从中心点辐射出来的值轴。线条用于连接系列值，并且轴的相对位置或角度通常是不固定的。
Spread JS 支持三种类型的雷达图——1）基本雷达图 2）填充雷达图 3）带标记的雷达图。

## 示例

在下面展示的示例中，借助在电子表格中针对以下数据绘制的不同类型的雷达图，呈现了两名学生（丽莎和蒂姆）在六门不同学科（数学、英语、历史、生物、地理和动物学）的考试成绩分析：
![data-for-chart](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/5fefd5d8-238b-4c13-893b-6f22fcc5b9dd/data-for-chart.400042.png)

## 基本雷达图

下面是一张基本雷达图，展示了两名学生的考试成绩分析，通过不同的线条颜色突出显示了每个学生的区域：

## 填充雷达图

下面是一张填充雷达图，展示了两名学生的考试成绩分析，通过不同颜色的区域突出显示了每个学生的区域：
![radar-filled](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/5fefd5d8-238b-4c13-893b-6f22fcc5b9dd/radar-filled.e13bf0.png)

## 带标记的雷达图

下面是一张带标记的雷达图，展示了两名学生的考试成绩分析，标记表示每门学科的分数，同时通过不同的线条颜色突出显示了每个学生的区域：
![marker-radar](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/5fefd5d8-238b-4c13-893b-6f22fcc5b9dd/marker-radar.d47a69.png)
以下代码示例展示了如何在工作表中创建基本雷达图、填充雷达图和带标记的雷达图，以分析两名学生的成绩。

```javascript
// 初始化 Spread
var spread = new GC.Spread.Sheets.Workbook(document.getElementById('ss'), { sheetCount: 3 });
// 获取活动工作表
var activeSheet = spread.getSheet(0);
// 添加图表数据
var dataArray = [
    ['', '丽莎', '蒂姆'],
    ['数学', 87, 64],
    ['英语', 79, 59],
    ['历史 ', 58, 75],
    ['生物', 64, 96],
    ['地理', 49, 85],
    ['动物学', 64, 83],
];
activeSheet.setArray(1, 1, dataArray);
activeSheet.setColumnWidth(0, 20);
for (var i = 1; i < 4; i++) {
    activeSheet.setColumnWidth(i, 100);
}
activeSheet.options.gridline.showHorizontalGridline = false;
activeSheet.options.gridline.showVerticalGridline = false;
activeSheet.getRange(1, 1, 7, 3).hAlign(GC.Spread.Sheets.HorizontalAlign.center).setBorder(new GC.Spread.Sheets.LineBorder('black', GC.Spread.Sheets.LineStyle.thin), { all: true });
activeSheet.getRange(1, 2, 1, 3).font('bold normal 10pt Arial');
activeSheet.getRange(2, 1, 6, 1).font('bold normal 10pt Arial');

// 添加雷达图
// 1 - 雷达图
// 2 - 填充雷达图
// 3 - 带标记的雷达图
var chart = activeSheet.charts.add('RadarChart', GC.Spread.Sheets.Charts.ChartType.radar, 250, 0, 500, 410, '$B$2:$D$7');
var legend = chart.legend();
legend.visible = true;
legend.position = GC.Spread.Sheets.Charts.LegendPosition.bottom;
chart.legend(legend);

var chartArea = chart.chartArea();
chartArea.fontSize = 14;
chart.chartArea(chartArea);

var dataLabels = chart.dataLabels();
dataLabels.showValue = true;
dataLabels.showSeriesName = false;
dataLabels.showCategoryName = false;
dataLabels.position = GC.Spread.Sheets.Charts.DataLabelPosition.above;
chart.dataLabels(dataLabels);

var title = chart.title();
title.text = '考试成绩分析';
title.fontSize = 22;
title.color = '#b20000';
title.fontFamily = 'Cambria';
chart.title(title);

var axes = chart.axes();
axes.primaryCategory.style.color = '#000000';
axes.primaryValue.majorGridLine.color = '#ccc';
chart.axes(axes);

var series = chart.series();
var lisaSeries = series.get(0);
lisaSeries.border.color = 'skyblue';
lisaSeries.backColor = 'skyblue';
lisaSeries.backColorTransparency = '0.5';
series.set(0, lisaSeries);
var timSeries = series.get(1);
timSeries.border.color = 'orange';
timSeries.backColor = 'orange';
timSeries.backColorTransparency = '0.5';
series.set(1, timSeries);
```