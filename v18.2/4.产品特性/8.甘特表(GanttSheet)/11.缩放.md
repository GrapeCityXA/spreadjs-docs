# zoom

您可以调整甘特图表的大小，以便关注特定任务或整个项目。
为此，您需要使用 `timescale` 类的以下方法执行各种缩放操作。

* `zoomFactor`：获取或设置时间刻度的缩放因子。
* `zoomIn()`：将时间刻度放大到下一个级别。
* `zoomOut()`：将时间刻度缩小到下一个级别。
* `zoomAuto()`：根据每个层级上刻度标签的情况，自动将时间刻度缩放到合适的因子。
* `zoomTo()`：将时间刻度缩放到指定的因子。
* `zoomToRange()`：将时间刻度缩放到在视口区域内显示目标日期范围。

以下示例代码展示了对时间刻度进行各种缩放操作的方法。

```javascript
// Get the zoom factor.   
    var timescale = ganttSheet.project.timescale;   
    console.log(timescale.zoomFactor);
// Zoom in the timescale.
   var timescale = ganttSheet.project.timescale;
   timescale.zoomIn();
// Zoom out the timescale.
   var timescale = ganttSheet.project.timescale;
   timescale.zoomOut();
// Auto zoom the timescale.
   var timescale = ganttSheet.project.timescale;
   timescale.zoomAuto();
// Zoom the timescale to the specified factor.
   var timescale = ganttSheet.project.timescale;
   var zoomFactor = 2;   timescale.zoomTo(zoomFactor);
// Zoom the timescale to the specified range.
   var timescale = ganttSheet.project.timescale;
   var startDate = new Date(2023, 7, 6);
   console.log(startDate);
   var endDate = new Date(2023, 7, 13);
   console.log(endDate);
   timescale.zoomToRange(startDate, endDate);
```

下面的 Gif 演示了甘特图中不同的缩放操作。
![TimescaleZoom.33986f](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/b33c4f64-6b99-4132-a675-8dc7b59765f1/TimescaleZoom.33986f.4c4eb7.gif?width=1000)