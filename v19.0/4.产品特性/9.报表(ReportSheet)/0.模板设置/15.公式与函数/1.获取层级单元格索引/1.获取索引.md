# get-index

这个功能用于获取溢出单元格的索引。

## 语法

`R.INDEX(cell, [context_cell])`

## 参数

此函数具有以下参数：

| **参数** | **描述** |
| --- | --- |
| *cell* | [必需] 指定您想要获取索引的单元格。 |
| *context\_cell* | [可选] 指定上下文单元格。如果已指定上下文单元格，此函数将返回上下文中的单元格索引。 |

## 示例

```javascript
// Use the formula(R.Index) to get the index of the spilled cells.
templateSheet.setFormula(1, 3, "R.INDEX(C2)");
templateSheet.setFormula(1, 4, "R.INDEX(C2,A2)");
templateSheet.setFormula(1, 5, "R.INDEX(A2)");
```

输出如下：
![image](/DOCUMENT_SITE_LINK_PREFIX_HERE/document-site-files/images/ca82939a-5d1a-4838-9f3f-69a0f7ebeeb5/image.3e0be7.png)